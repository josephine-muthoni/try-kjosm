{
  "name": "dimmasparksplus-communication-monolith",
  "version": "1.0.0",
  "description": "Monolithic web application for phones and accessories.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:frontend": "cd src/frontend && npm install && npm run build",
    "install:all": "npm install && cd src/frontend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^8.1.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "mongoose": "^8.16.3",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
